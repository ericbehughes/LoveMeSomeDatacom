<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.lovemesomedatacom">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".classcancelation.ClassCancellationActivity" />

        <activity android:name=".findteacher.FindTeacherActivity" />
        <activity android:name=".findteacher.ChooseTeacherActivity" />
        <activity android:name=".findteacher.TeacherContactActivity" />
        <activity android:name=".business.MenuActivity" />
        <activity android:name=".weather.WeatherActivity" />
        <activity android:name=".about.AboutActivity" />
        <activity android:name=".settings.SettingsActivity" />
        <activity android:name=".business.NetworkActivity" />
        <activity android:name=".business.NetworkFactoryActivity" />
        <activity android:name=".notes.NotesActivity" />
        <activity android:name=".notes.NoteDetailsActivity" />
        <activity android:name=".calendar.CalendarActivity" />
        <activity android:name=".academiccalendar.AcademicCalendar" />

        <activity android:name=".findfriend.FindFriendsActivity" />
        <activity android:name=".weather.TemperatureLocation"></activity>
        <activity android:name=".findfriend.WhosFreeActivity" />
        <activity android:name=".findfriend.FriendsByCourseListActivity"></activity>
    </application>

</manifest>